<br><br>
<h1 class="text-center">メンバー（一覧）</h1>
<div class="mx-auto justify-content-center" style="width: 500px;">
  <table class="table">
    <thead>
      <tr>
        <th>#</th>
        <th>氏名</th>
        <th>メールアドレス</th>
      </tr>
    </thead>
    <tbody>
      <% @members.each do |member| %>
        <tr>
          <td><%= member.id %></td>
          <td><%= member.name %></td>
          <td><%= member.email %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>